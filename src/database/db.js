const mysql = require("mysql2/promise");
const config = require("../config/mysql");

async function query(sql, params) {
  /**
   * @todo create a database connection and perform queries
   */
}

module.exports = {
  query,
};
